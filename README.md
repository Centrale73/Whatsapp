# Agno-Perplexity WhatsApp Agent

A developer-native AI agent using the **Agno framework** and **Perplexity AI**, integrated with **WhatsApp via Twilio**.

## 🚀 Setup
1. **Clone**: `git clone https://github.com/Centrale73/Whatsapp.git`
2. **Install**: `pip install -r requirements.txt`
3. **Env**: Set `PERPLEXITY_API_KEY`, `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`.
4. **Run**: `python app.py`
5. **Expose**: Use `ngrok http 5000` for the webhook.
